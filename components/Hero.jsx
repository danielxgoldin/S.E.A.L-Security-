import styles from "../style";
//importing videos and images from react
import { discount, robot, seal, banner} from "../assets";
import ReactPlayer from "react-player";

const Hero = () => (

  <section style={{backgroundImage:`url(${banner})`}} id="home" className={`flex md:flex-row flex-center ${styles.paddingY}`} >
   <div>
    
   </div>


    
  </section>

  
);

export default Hero;
